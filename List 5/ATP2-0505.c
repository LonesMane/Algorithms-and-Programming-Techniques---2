//Escreva uma funcao recursiva para verificar se um n ́numero  ́e primo.//
#include<stdio.h>
#include<stdlib.h>

int test_primo (int num)
{
    int divisores;

    if (num == 0)
        return -1;

    if (num == 1)
        
}
int main()
{
    int num;

    //Coletando o numero do usuario para verificar se é primo//
    printf("Calcular quantos termos: ");
    scanf("%d", &num);
}